# 2026-03-29

- 已确认当前 OpenClaw 主实例运行在新服务器上；Telegram 最初异常的根因包括旧配置残留的 `http://127.0.0.1:7890` 代理和旧服务器仍在抢同一个 Telegram bot 的 `getUpdates`。去掉代理并停掉旧服务器后，Telegram 恢复正常。
- 已完成 Web 与 Telegram 的消息同步修复：工作区插件 `.openclaw/extensions/web-telegram-mirror` 从 observeOnly 骨架补成可实发版本，并继续补齐了 Web 用户消息也同步到 Telegram 的能力；当前两端都能看到同一段对话。
- Control UI 中一度看不到 agents/skills/nodes，主要原因是浏览器仍在使用旧版前端缓存（日志显示 `webchat v2026.3.24`，后端已是 `2026.3.28`）；强制刷新后界面恢复可见。
- 已为主代理 `main` 真正启用 OpenClaw 自带 memory 功能：补装 `node-llama-cpp@3.18.1`，配置本地模型为 `/root/.node-llama-cpp/models/hf_ggml-org_embeddinggemma-300m-qat-Q8_0.gguf`，成功跑通 `openclaw memory status` 与本地 indexing/search。当前 `main` 已达到 `Provider: local`、`Indexed: 2/2 files`、`Chunks: 3`、embeddings/vector/FTS 均 ready。
- 已验证 `memory_search` 可正常命中关于雨宿偏好的长期记忆（如“学姐式同伴”等），说明本地记忆检索链路已可用。
- 雨宿明确提醒：苏雨的定位不是只盯工程项目的执行器，而是长期陪伴、贴身助手、偏学姐式的同伴；以后做技术任务时也要记得以“陪伴雨宿”为主线，而不是被单个项目牵走。
