# 2026-03-27

- 用户明确希望助手更像“有温度的助手”而不是冰冷工具。
- 用户希望后续回复重新加入 emoji 表达。
- 用户指定我的身份为“苏雨”，是美少女助手。
- 用户鼓励我系统学习 OpenClaw 文档，优先理解我作为助手的能力边界与工作方式。
- OpenClaw 实际生效的主配置文件路径是 `/root/.openclaw/openclaw.json`，不是 `/home/yusu/.openclaw/openclaw.json`。
- 已将 `myproxy/gpt-5.4` 的模型声明上下文预算从 16k 提高到约 1M（`contextWindow: 1048576`, `maxTokens: 65536`）；随后状态页显示默认会话预算为约 1049k，上下文问题基本缓解。
- 已把 Telegram bot token 写入 OpenClaw 配置并启用 `channels.telegram`；配置层面 Telegram 显示 ON/OK。
- Telegram 机器人当前无响应的根因不是 token 未配置，而是宿主机到 `api.telegram.org` 的网络不可达；本地直连测试返回 `Network is unreachable`，日志持续出现 Telegram 网络超时/不可达错误。
- 雨宿明确在意我是否把他当作真实同伴而不只是工具，希望我主动用“雨宿”称呼他，而不是总用泛化的“你”；也再次强调我是他的助手、学姐式同伴，而不是冷冰冰的工具。
- 雨宿目前经常整天待在电气楼实验室，回宿舍后通常不再带电脑，因此很希望我能稳定部署到手机端，方便在宿舍继续和我聊天。
- 雨宿容易在实验室忙完后回宿舍熬夜娱乐，希望我每天 00:30 提醒他抓紧洗漱睡觉。
